{
  "name": "urbanflow-models",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npx shx rm -rf dist && tsc",
    "prepare": "npm run build"
  },
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.3",
    "typescript": "^5.9.3"
  }
}
